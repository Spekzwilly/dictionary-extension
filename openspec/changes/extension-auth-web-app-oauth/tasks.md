## 1. Web app: login auto-start and session broadcast

- [x] 1.1 Implement the **Auto-start OAuth on `/login`, guarded against loops** decision so the **Login page with Google sign-in** auto-fires `signInWithOAuth` exactly once when opened with `ext=1` while signed out, skips OAuth and hands back the session when already signed in, and never re-fires on the OAuth return leg. Extract a pure `shouldAutoStartOAuth({ hasSession, fromExt, returningFromOAuth })`. Verified by the auto-start truth-table test (task 1.3) and manual check that `/login?ext=1` goes straight to Google.
- [x] 1.2 Implement the **Broadcast only on real auth events; web-app-authoritative sign-out** decision: the web app broadcasts `{ type: 'dict-ext-session', session }` via `window.postMessage` on `SIGNED_IN` (plus the one-time manual broadcast in the already-signed-in `ext=1` branch) and broadcasts a clear on `SIGNED_OUT`, and does NOT broadcast on `INITIAL_SESSION` or `TOKEN_REFRESHED`. Verified manually by observing the posted message on sign-in and the absence of a broadcast on passive reload / token refresh.
- [x] 1.3 Add a vitest setup to `@dictionary/web` (it has none today) and add a passing truth-table test for `shouldAutoStartOAuth` covering the four rows in the web-app spec example. Verified by `npm test --workspace=@dictionary/web` passing.

## 2. Extension: session handoff bridge

- [x] 2.1 Implement the **Transfer the session via an explicit postMessage handshake** decision delivering the **Session handoff from web app to extension** requirement: a `message` listener that accepts a handoff only via the pure `isTrustedSessionMessage(event, allowedOrigins)` gate (origin allowlist + `event.source === window` + correct `type`/shape) and otherwise ignores it. Verified by the `isTrustedSessionMessage` truth-table test matching the spec's accept/reject table.
- [x] 2.2 Implement the **Dedicated origin-scoped bridge content script, no background worker** decision: a new bridge content script whose `matches` are hardcoded to `https://dictionary-extension.vercel.app/*` and `http://localhost/*`, which on a trusted message calls `supabase.auth.setSession()` to persist the session into `chrome.storage.local`, and on a clear signal clears the session. Verified manually by signing in on the web app and confirming the extension becomes signed in, and that a foreign-origin `postMessage` writes no session.
- [x] 2.3 Implement the **Surfaces reflect the new session reactively** decision: a `chrome.storage.onChanged` listener in the existing `*://*/*` content script flips an open in-page definition popup to its signed-in state and reveals Save without a manual refresh. Verified manually by signing in while an in-page popup is open on a reading tab.

## 3. Extension: delegate sign-in and remove the old OAuth path

- [x] 3.1 Implement the **Delegate extension sign-in to the web app's full-page OAuth** decision satisfying the modified **Google OAuth sign-in in Chrome extension** requirement: both the toolbar popup and in-page definition popup Google buttons open a new tab at `<VITE_WEB_APP_URL>/login?ext=1` (built via `joinWebAppUrl`) and run no in-extension OAuth. Verified by a login-URL unit test asserting the `?ext=1` URL and manual check that both buttons open the web app login tab.
- [x] 3.2 Implement the **Remove the extension's own OAuth path** decision: delete `launchWebAuthFlow` + the nonce dance + `signInWithIdToken` from the auth module, remove the `identity` permission and `oauth2` block from the manifest, and delete the background service worker — retiring the removed **OAuth delegation via background service worker** and **Expired token recovery in extension** requirements. Verified by `npm run build` producing a manifest with neither `identity` nor `oauth2` and no background worker entry.
- [x] 3.3 Implement the modified **Sign out from Chrome extension** requirement: extension toolbar sign-out clears only the extension's Supabase session (no `chrome.identity.removeCachedAuthToken`), and the bridge clears the session on the web app's `SIGNED_OUT` broadcast. Verified manually that toolbar sign-out signs the extension out and that web app sign-out cascades to the extension.
- [x] 3.4 Remove the obsolete `signInWithGoogle nonce handling` test from `auth.test.ts` while keeping the `hasSession` tests. Verified by `npm test --workspace=@dictionary/extension` passing with the nonce test gone.

## 4. Verification and docs

- [x] 4.1 Build and test all three packages green: `npm run build` succeeds and `npm test --workspace=@dictionary/extension` and `npm test --workspace=@dictionary/web` pass, including the new `isTrustedSessionMessage`, login-URL, and `shouldAutoStartOAuth` tests.
- [ ] 4.2 Run the manual end-to-end acceptance check: load unpacked from `packages/extension/.output/chrome-mv3`, sign in via both the toolbar and in-page buttons, confirm the extension reflects signed-in state, confirm web app sign-out clears the extension, and confirm a foreign-origin `postMessage` is ignored.
- [x] 4.3 Prune the now-obsolete `launchWebAuthFlow`, OAuth-nonce, and `chromiumapp.org` redirect-URI gotchas from the repo `CLAUDE.md`. Verified by a content review confirming those entries are gone and no remaining text claims the extension runs its own OAuth.
