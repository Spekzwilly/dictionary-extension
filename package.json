{
  "name": "dictionary-monorepo",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/extension",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspace=@dictionary/shared && npm run build --workspace=@dictionary/extension && npm run build --workspace=@dictionary/web"
  }
}
